---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full px-4 sm:px-6 md:px-8 lg:px-0 mx-auto container max-w-full sm:max-w-3xl md:max-w-2xl lg:max-w-4xl my-12 sm:my-16 md:my-24 lg:my-28`}
>
  <slot />
</section>
